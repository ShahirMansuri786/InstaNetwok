<%= form_with(model: room) do |form| %>
  <% if room.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
        <% room.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.hidden_field :created_by , value: current_user.id %>
    <%= form.select :created_for, options_from_collection_for_select(User.all, :id, :name), { include_blank: 'Select Friend' }, { class: "form-controller btn btn-outline-success btn-sm mt-2" } %>
    <%= form.submit "AddFriend" , class: "btn btn-outline-success btn-sm mt-2" %>
  </div>
<% end %>
